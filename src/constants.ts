// Default error validators and error messages in case of missing custom ones.
export const REQUIRED_REGEX = /\S/;
export const REQUIRED_ERROR = 'This field is required.';
export const INVALID_FIELD = 'This field is not valid.';

// Default option values for the main useForm hook.
export const DEFAULT_OPTIONS = {
  PREVENT_DEFAULT_EVENT_ON_SUBMIT: true,
};
